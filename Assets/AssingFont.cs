using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class AssingFont : MonoBehaviour
{
    public TMP_FontAsset JostHeavyEngAndRuss;
    public TMP_FontAsset notoSansChineseBold;

    public void Update()
    {
       /*if(LocalizationStrings.changeFont == 1)
        {
            if (LocalizationStrings.ChooseText == 1 || LocalizationStrings.ChooseText == 2)
            {
                gameObject.transform.GetChild(0).GetComponent<TextMeshProUGUI>().font = JostHeavyEngAndRuss;
            }
            else
            {
                gameObject.transform.GetChild(0).GetComponent<TextMeshProUGUI>().font = notoSansChineseBold;
            }

            LocalizationStrings.changeFont = 0;

        }
       */
    }
}
